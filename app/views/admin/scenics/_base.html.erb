<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(index).include?(action_name), t('links.scenics.new') , new_admin_scenic_path, class: 'btn btn-success' %>
      <%= link_to_if %w(new create edit update).include?(action_name), t('links.scenics.index'), admin_scenics_path, class: 'btn btn-success' %>
    </div>
  </div>
</div>