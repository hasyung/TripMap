<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(new create edit update).include?(action_name), 
                     t('links.images.index_back', :model => @model.name), @path, :class => 'btn btn-success' %>
    </div>
  </div>
</div>