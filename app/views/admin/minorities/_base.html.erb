<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(index).include?(action_name), t('links.minorities.new'), new_admin_special_minority_path(params[:special_id]), class: 'btn btn-success' %>
      <%= link_to_if %w(new create edit update show).include?(action_name), t('links.minorities.index'), admin_special_minorities_path(params[:special_id]), class: 'btn btn-success' %>
    </div>
  </div>
</div>