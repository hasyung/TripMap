<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(index).include?(action_name), t('links.nicknames.new'), new_admin_nickname_path, :class => 'btn btn-success' %>
      <%= link_to_if %w(show new create edit update).include?(action_name), 
                     t('links.nicknames.index'), admin_nicknames_path, :class => 'btn btn-success' %>
    </div>
  </div>
</div>