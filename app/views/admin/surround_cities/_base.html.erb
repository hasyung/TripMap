<div class="toolbar">
	<div class="btn-toolbar">
		<div class="btn-group">

			<%= link_to_if %w(index).include?(action_name),
					t('links.surround_cities.new'),
					new_admin_surround_city_path,
					:class => 'btn btn-success' %>

			<%= link_to_if %w(new create edit update).include?(action_name),
					t('links.surround_cities.index'),
					admin_surround_cities_path,
					:class => 'btn btn-success' %>

		</div>
	</div>
</div>