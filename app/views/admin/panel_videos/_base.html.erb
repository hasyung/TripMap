<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">

      <%= link_to_if %w(index).include?(action_name), t('links.panel_videos.new'), new_admin_panel_video_path, class: 'btn btn-success' %>
      <%= link_to_if %w(new create edit update).include?(action_name), t('links.panel_videos.index'), admin_panel_videos_path, class: 'btn btn-success' %>

    </div>
  </div>
</div>