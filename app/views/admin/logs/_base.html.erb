<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(select).include?(action_name), 
                     t('links.logs.index'), admin_logs_path, :class => 'btn btn-success' %>
    </div>
  </div>
</div>
