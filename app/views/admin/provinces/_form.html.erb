<%= simple_form_for([:admin,@province], html: { class: 'form-horizontal' }) do |f| %>
	<%= f.input :name %>
	<%= f.input :slug %>
	<div class="form-actions">
		<%= f.button :submit, class: 'btn-primary' %>
	</div>
<% end %>