<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(index).include?(action_name), t('links.provinces.new'),new_admin_province_path, class: 'btn btn-success' %>
      <%= link_to_if %W(new create edit update).include?(action_name), t('links.provinces.index'), admin_provinces_path, class: 'btn btn-success' %>
    </div>
  </div>
</div>