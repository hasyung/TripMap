<%= simple_form_for [:admin, @account] do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :nickname %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
  </div>
<% end %>