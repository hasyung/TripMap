<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(new create edit update).include?(action_name), 
                     t('links.minority_slides.index'), admin_special_minority_path(params[:special_id], params[:minority_id]), :class => 'btn btn-success' %>
    </div>
  </div>
</div>