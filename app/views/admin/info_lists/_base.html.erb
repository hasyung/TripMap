<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(index).include?(action_name), t('links.info_lists.new'), new_admin_info_list_path, :class => 'btn btn-success' %>
      <%= link_to_if %w(new create edit update).include?(action_name), t('links.info_lists.index'), admin_info_lists_path, :class => 'btn btn-success' %>
    </div>
  </div>
</div>