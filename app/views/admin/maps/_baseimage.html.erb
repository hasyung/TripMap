<div class="toolbar">
  <div class="btn-toolbar">
    <div class="btn-group">
      <%= link_to_if %w(new_image edit_image).include?(action_name), 
                     t('links.maps.images.back'), edit_admin_map_path(params[:map_id]), :class => 'btn btn-success' %>
    </div>
  </div>
</div>