<div class="toolbar">
  <div class="btn-toolbar">
    <div clss="btn_group">
      <%= link_to_if %w(index).include?(action_name), 
          t('links.recommend_records.new'), 
          new_admin_recommend_record_path(params[:recommend_id]), 
          class: "btn btn-success" %>
      <%= link_to_if %w(index).include?(action_name), 
          t('links.recommend_records.back'), 
          admin_recommends_path, 
          class: "btn btn-success backinfo" %>
      <%= link_to_if %w(new create edit update).include?(action_name), 
          t('links.recommend_records.back'), 
          admin_recommend_records_path(params[:recommend_id]), 
          class: "btn btn-success" %>
    </div>
  </div>
</div>