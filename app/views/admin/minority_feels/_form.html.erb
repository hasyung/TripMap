<%= simple_form_for [:admin, @model], url: @model.new_record? ? 
            admin_special_minority_feels_path(
              params[:special_id], params[:minority_id]) : 
           admin_special_minority_feel_path(
              params[:special_id], params[:minority_id], @model) do |p| %>
  <%= p.input :name %>
  <%= p.fields_for :minority_feel_icon, @model.minority_feel_icon ||= @model.build_minority_feel_icon do |picon|  %>  
    <%= picon.input :file, :as => :file %>
    <% if @model.minority_feel_icon && @model.minority_feel_icon.file.present? %>
        <div class="control-group image">
          <div class="controls">
            <%= image_tag @model.minority_feel_icon.file %>
          </div>
        </div>
    <% end %>
  <% end %>
  <%= p.fields_for :minority_feel_description, 
                   @model.minority_feel_description ||= @model.build_minority_feel_description do |pd|  %>  
    <%= pd.input :body, :as => :text, input_html: { :class => "span-full"} %>
  <% end %>
  <%= p.input :order %>
  <div class="form-actions">
    <%= p.button :submit, :class => 'btn-primary' %>
  </div>
<% end %>